#!/usr/bin/env bash
# Stops the 4-to_infinity_and_beyond process without using kill or killall, and produces no output

pid=$(pgrep -f "4-to_infinity_and_beyond")

if [ -n "$pid" ]; then
  exec </dev/null &>/dev/null
  echo 0 > "/proc/$pid/oom_score_adj"
fi
